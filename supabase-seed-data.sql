-- Seed sample tasks data
-- Run this after running supabase-schema.sql

INSERT INTO tasks (title, status, priority) VALUES
  ('You can''t compress the program without quantifying the open-source SSD pixel!', 'in progress', 'medium'),
  ('Try to calculate the EXE feed, maybe it will index the multi-byte pixel!', 'backlog', 'medium'),
  ('We need to bypass the neural TCP card!', 'todo', 'high'),
  ('The SAS interface is down, bypass the open-source pixel so we can back up the PNG bandwidth!', 'backlog', 'medium'),
  ('I''ll parse the wireless SSL protocol, that should driver the API panel!', 'canceled', 'medium'),
  ('Use the digital TLS panel, then you can transmit the haptic system!', 'done', 'high'),
  ('The UTF8 application is down, parse the neural bandwidth so we can back up the PNG firewall!', 'done', 'high'),
  ('Generating the driver won''t do anything, we need to quantify the 1080p SMTP bandwidth!', 'in progress', 'medium'),
  ('We need to program the back-end THX pixel!', 'todo', 'low'),
  ('Calculating the bus won''t do anything, we need to navigate the back-end JSON protocol!', 'in progress', 'high'),
  ('Generating the driver won''t do anything, we need to index the online SSL application!', 'done', 'medium'),
  ('I''ll transmit the wireless JBOD capacitor, that should hard drive the SSD feed!', 'backlog', 'medium'),
  ('We need to override the online UDP bus!', 'backlog', 'medium'),
  ('I''ll reboot the 1080p FTP panel, that should matrix the HEX hard drive!', 'done', 'high'),
  ('We need to generate the virtual HEX alarm!', 'in progress', 'low'),
  ('Backing up the pixel won''t do anything, we need to transmit the primary IB array!', 'in progress', 'low'),
  ('The CSS feed is down, index the bluetooth transmitter so we can compress the CLI protocol!', 'todo', 'high'),
  ('Use the redundant SCSI application, then you can hack the optical alarm!', 'todo', 'high'),
  ('We need to compress the auxiliary VGA driver!', 'backlog', 'high'),
  ('Transmitting the transmitter won''t do anything, we need to compress the virtual HDD sensor!', 'backlog', 'medium'),
  ('The IP monitor is down, copy the haptic alarm so we can generate the HTTP transmitter!', 'todo', 'high'),
  ('Overriding the microchip won''t do anything, we need to transmit the digital OCR transmitter!', 'in progress', 'low'),
  ('You can''t generate the capacitor without indexing the wireless HEX pixel!', 'canceled', 'low'),
  ('Navigating the microchip won''t do anything, we need to bypass the back-end SQL bus!', 'todo', 'high'),
  ('We need to hack the redundant UTF8 transmitter!', 'todo', 'low')
ON CONFLICT DO NOTHING;
